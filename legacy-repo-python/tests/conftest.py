import datetime
import pytest
import sys
import os

# Ensure the legacy-repo-python/ directory is on the path so `clinic` is importable.
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..'))
